/**
 * Merge Sort
 * 
 * * Divide the array into sub array, each containing only one element
 * * repeatedly merge the sub array to produce new sorted subArray until there is only sub array is remaining,
 */

function mergeSort(nums = [])
{
    let leftArray = [], rightArray = []
    
}